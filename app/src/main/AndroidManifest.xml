<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hnl.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon"
        android:label="Shoes"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signUp" />
        <activity android:name=".ShoeList" />
        <activity android:name=".shoes_list" />
        <activity android:name=".shoesDetail" />
        <activity android:name=".Shoe_Detail" />
        <activity android:name=".main_category" />
        <activity android:name=".load_list_shoe_request"></activity>
    </application>

</manifest>